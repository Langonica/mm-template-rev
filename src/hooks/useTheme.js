/**
 * useTheme Hook - Re-export from ThemeContext
 * 
 * This file provides a clean import path for the useTheme hook.
 * Import from here instead of contexts/ThemeContext for cleaner code.
 * 
 * Usage:
 *   import { useTheme, useThemeStyles, THEMES } from './hooks/useTheme';
 */

export { useTheme, useThemeStyles, THEMES } from '../contexts/ThemeContext';
